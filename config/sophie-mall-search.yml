spring:
  datasource:
    url: jdbc:mysql://localhost:3306/sophie_mall?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai&useSSL=false
    username: sophie
    password: 12345678
  elasticsearch:
    uris: localhost:9200

logging:
  level:
    root: info
    com:
      sophie:
        sophiemall: debug

logstash:
  host: localhost